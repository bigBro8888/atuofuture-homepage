# 产品详情「实际案例」区块

将原「系统位置」堆叠图替换为实际案例区。

## 结构
- `story.cases = { title, items: [{ title, desc, image } × 3] }`
- 前台：区标题 + 一排三列卡片（上图、下标题与介绍）
- 不保留 ASpace 链接
- 锚点 `#hpi-cases`（旧 `#hpi-system` 废弃）

## 后台
- 可视化编辑：点选改标题/介绍/图片
- 建议尺寸约 `560×360`
