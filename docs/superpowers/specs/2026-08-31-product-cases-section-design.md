# 产品详情「实际案例」区块

将原「系统位置」堆叠图替换为实际案例区。

## 结构
- `story.cases = { title, items: [{ title, desc, image } × 3] }`
- 前台：区标题 + 三段纵向案例（上文案、下大图）
- 不保留 ASpace 链接
- 锚点 `#hpi-cases`（旧 `#hpi-system` 废弃）

## 后台
- 可视化编辑：点选改标题/介绍/图片
- 建议尺寸约 `1680×900`
